# Example secrets file for Streamlit
# Copy this to .streamlit/secrets.toml and fill in your values
# Note: .streamlit/secrets.toml should never be committed to version control

# Example API key (if needed in future enhancements)
# api_key = "your-api-key-here"

# Database configuration (if using database in future)
# db_host = "localhost"
# db_port = 5432
# db_name = "movies_db"
# db_user = "user"
# db_password = "password"
